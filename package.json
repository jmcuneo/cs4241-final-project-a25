{
  "name": "mbta-live",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"pnpm --filter mbta-live-server dev\" \"pnpm --filter mbta-live-client dev\"",
    "build": "pnpm -w --filter mbta-live-client build && pnpm -w --filter mbta-live-server build",
    "start": "pnpm --filter mbta-live-server start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "packageManager": "pnpm@9.11.0"
}
